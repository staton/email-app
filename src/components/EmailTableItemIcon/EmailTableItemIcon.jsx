import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};

const defaultProps = {
};

class EmailTableItemIcon extends React.Component {

    render() {

    }
}

EmailTableItemIcon.propTypes = propTypes;
EmailTableItemIcon.defaultProps = defaultProps;

export default EmailTableItemIcon;