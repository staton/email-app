import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};

const defaultProps = {
};

class EmailViewerContent extends React.Component {

    render() {

    }
}

EmailViewerContent.propTypes = propTypes;
EMailViewerContent.defaultProps = defaultProps;

export default EmailViewerContent;