// https://reactjs.org/docs/composition-vs-inheritance.html
import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};

const defaultProps = {
};

class EmailViewerActionBar extends React.Component {

    render() {

    }
}

EmailViewerActionBar.propTypes = propTypes;
EmailViewerActionBar.defaultProps = defaultProps;

export default EmailViewerActionBar;