import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};

const defaultProps = {
};

class EmailViewer extends React.Component {

    render() {

    }
}

EmailViewer.propTypes = propTypes;
EmailViewer.defaultProps = defaultProps;

export default EmailViewer;