import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};

const defaultProps = {
};

class ComposeEmailForm extends React.Component {

    render() {
        return (
            <div className="ComposeEmailForm">
                compose email
            </div>
        );
    }
}

ComposeEmailForm.propTypes = propTypes;
ComposeEmailForm.defaultProps = defaultProps;

export default ComposeEmailForm;